name: harp_tuner
description: "Harpie — a warm, accurate chromatic tuner for lever and pedal harps."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.4.0+29

environment:
  sdk: ^3.11.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

  cupertino_icons: ^1.0.9
  shared_preferences: ^2.5.5
  google_fonts: ^8.1.0
  flutter_riverpod: ^3.3.1
  audioplayers: ^6.8.1
  mic_stream: ^0.7.0
  pitch_detector_dart: any
  permission_handler: ^12.0.1
  wakelock_plus: ^1.8.0

  # Anonymous in-app feedback (see lib/services/feedback_service.dart).
  # Firebase init is guarded in main.dart; missing config degrades to a no-op.
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.0
  package_info_plus: ^10.2.1
  device_info_plus: ^13.2.0

dependency_overrides:
  # mic_stream 0.7.2 (pub.dev, unmaintained) imports the removed v1-embedding
  # class io.flutter.plugin.common.PluginRegistry.Registrar, which no longer
  # exists in current Flutter SDKs and breaks the Android build. The import
  # was dead code (never used), so we vendor a patched copy with it removed.
  mic_stream:
    path: third_party/mic_stream
  # Force permission_handler to 12.x for iOS 26 compatibility.
  # mic_stream declares ^10.0.0 but uses no APIs that changed in v12.
  # permission_handler_apple >= 9.4.8 ships Swift Package Manager support.
  permission_handler: ^12.0.1
  permission_handler_android: ^13.0.1
  permission_handler_apple: ^9.6.1
  permission_handler_platform_interface: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.1
  shared_preferences_platform_interface: ^2.4.0
  fake_async: ^1.3.3
  # Test-only: lets screen_wake_service_test swap the wakelock platform.
  # Tracks wakelock_plus's own ^1.2.3 rather than pinning higher, so this dev
  # dep never dictates resolution. (The Flutter floor still moved to >=3.41.0 —
  # pub resolves the interface to 1.6.0 for wakelock_plus regardless.)
  wakelock_plus_platform_interface: ^1.2.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"
  min_sdk_android: 21
  web:
    generate: false
  windows:
    generate: false
  macos:
    generate: false

flutter:
  generate: true

  # Keep iOS on CocoaPods. With Swift Package Manager on, Firebase ships its
  # Firestore/gRPC/abseil/BoringSSL pieces as prebuilt frameworks declaring
  # MinimumOSVersion 100.0, bypassing the Podfile's iOS 15.0 floor that App
  # Store Connect validation relies on (#44, #45). Migrate in its own change,
  # validated with a TestFlight upload.
  config:
    enable-swift-package-manager: false

  uses-material-design: true

  assets:
    - assets/icon/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
